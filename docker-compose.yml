version: "3.9"

services:
  voyager:
    build: .
    container_name: voyager
    restart: unless-stopped
    command: python main.py

    env_file:
      - .env

    ports:
      - "8080:8080"

    extra_hosts:
      - "host.docker.internal:host-gateway"

    stdin_open: true
    tty: true
